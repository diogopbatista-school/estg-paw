<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0" />
    <title>Detalhes do Menu</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" />
    <style>
      body {
        background: linear-gradient(to right, #4b6cb7, #182848);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Poppins", sans-serif;
        color: #fff;
      }
      .container {
        max-width: 1000px;
        background: rgba(255, 255, 255, 0.95);
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.3);
        color: #333;
      }
      h2 {
        color: #007bff;
        text-align: center;
        font-weight: 700;
        margin-bottom: 20px;
      }
      .btn {
        font-weight: bold;
        border-radius: 8px;
        transition: background 0.3s;
      }
      .btn-primary {
        background: #007bff;
        border: none;
      }
      .btn-primary:hover {
        background: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h2>Detalhes do Menu: <%= menu.category %></h2>

      <a
        href="/restaurants/manage/<%= restaurantId %>/menus"
        class="btn btn-secondary mb-3"
        >Voltar</a
      >
      <a
        href="/restaurants/manage/<%= restaurantId %>/menus/<%= menu._id %>/dishes/create"
        class="btn btn-success mb-3"
        >Adicionar Prato</a
      >

      <% if (dishes.length > 0) { %>
      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th>Nome do Prato</th>
            <th>Descrição</th>
            <th>Preços</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody>
          <% dishes.forEach(function (dish) { %>
          <tr>
            <td><%= dish.name %></td>
            <td><%= dish.description || "Sem descrição" %></td>
            <td>
              <% if (dish.prices) { %> <% dish.prices.forEach(function (price) { %> <%= price.dose %>: <%= price.price.toFixed(2) %>€<br />
              <% }); %> <% } else { %> Nenhum preço disponível <% } %>
            </td>
            <
            <td>
              <a
                href="/restaurants/manage/<%= restaurantId %>/menus/<%= menu._id %>/dishes/<%= dish._id %>/edit"
                class="btn btn-warning btn-sm"
                >Editar</a
              >
              <form
                action="/restaurants/manage/<%= restaurantId %>/menus/<%= menu._id %>/dishes/<%= dish._id %>/delete"
                method="POST"
                style="display: inline">
                <button
                  type="submit"
                  class="btn btn-danger btn-sm">
                  Remover
                </button>
              </form>
            </td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      <% } else { %>
      <p class="text-muted text-center">Nenhum prato associado a este menu.</p>
      <% } %>
    </div>
  </body>
</html>

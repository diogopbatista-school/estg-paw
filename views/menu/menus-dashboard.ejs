<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0" />
    <title>Menus do Restaurante</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" />
    <style>
      body {
        background: linear-gradient(to right, #4b6cb7, #182848);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Poppins", sans-serif;
        color: #fff;
      }
      .container {
        max-width: 1000px;
        background: rgba(255, 255, 255, 0.95);
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.3);
        color: #333;
      }
      h2 {
        color: #007bff;
        text-align: center;
        font-weight: 700;
        margin-bottom: 20px;
      }
      .btn {
        font-weight: bold;
        border-radius: 8px;
        transition: background 0.3s;
      }
      .btn-primary {
        background: #007bff;
        border: none;
      }
      .btn-primary:hover {
        background: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h2>Menus do Restaurante: <%= restaurant.name %></h2>

      <a
        href="/restaurants/manage/<%= restaurant._id %>"
        class="btn btn-secondary mb-3"
        >Voltar</a
      >
      <a
        href="/restaurants/manage/<%= restaurant._id %>/menu/create"
        class="btn btn-success mb-3"
        >Criar Menu</a
      >

      <% if (menus.length > 0) { %>
      <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th>Categoria</th>
            <th>Data de Criação</th>
            <th>Ações</th>
          </tr>
        </thead>
        <tbody>
          <% menus.forEach(function (menu) { %>
          <tr>
            <td><%= menu.category %></td>
            <td><%= new Date(menu.created_at).toLocaleDateString() %></td>
            <td>
              <td>
                <a
                  href="/restaurants/manage/<%= restaurant._id %>/menus/<%= menu._id %>"
                  class="btn btn-primary btn-sm"
                  >Abrir Menu</a
                >
                <form
                  action="/restaurants/manage/<%= restaurant._id %>/menus/<%= menu._id %>/delete"
                  method="POST"
                  style="display: inline;">
                  <button type="submit" class="btn btn-danger btn-sm">Remover</button>
                </form>
              </td>
            </td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      <% } else { %>
      <p class="text-muted text-center">Nenhum menu associado a este restaurante.</p>
      <% } %>
    </div>
  </body>
</html>

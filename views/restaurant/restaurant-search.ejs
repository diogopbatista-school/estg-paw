<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Restaurantes Validados</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" />
    <style>
      body {
        background: linear-gradient(to right, #ff7e5f, #feb47b);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: Arial, sans-serif;
        padding: 20px;
      }
      .container {
        max-width: 900px;
        display: flex;
        flex-direction: column;
        gap: 20px;
      }
      .panel {
        background: #ffffff;
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      }
      h2 {
        text-align: center;
        font-weight: bold;
      }
      .list-group-item {
        background: #fff3e0;
        border-radius: 8px;
        margin-bottom: 10px;
        padding: 15px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="panel">
        <h2>Restaurantes</h2>
        <% if (restaurants && restaurants.length > 0) { %>
          <ul class="list-group">
            <% restaurants.forEach(function (restaurant) { %>
              <li class="list-group-item">
                <h3><%= restaurant.name %></h3>
                <p><strong>Descrição:</strong> <%= restaurant.description || "Sem descrição disponível" %></p>
                <p><strong>Localização:</strong> Latitude: <%= restaurant.location.latitude %>, Longitude: <%= restaurant.location.longitude %></p>
                <p><strong>Telefone:</strong> <%= restaurant.phone %></p>
              </li>
            <% }); %>
          </ul>
        <% } else { %>
          <p class="text-muted text-center">Nenhum restaurante validado encontrado.</p>
        <% } %>
      </div>
      <div class="panel text-center">
        <a href="/" class="btn btn-secondary">Voltar</a>
      </div>
    </div>
  </body>
</html>